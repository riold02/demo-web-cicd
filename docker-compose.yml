version: '3.8'

services:
  web:
    build: .
    container_name: demo-web-app
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "com.example.description=Demo Web Application"
      - "com.example.version=1.0.0"
